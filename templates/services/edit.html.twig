{% extends 'base.html.twig' %}

{% block title %}Modification Services{% endblock %}

{% block body %}
<div class="top container">

    <h1>Modification de {{ service.denomination }}</h1>

    {{ include('services/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('services_index') }}">Retour à la liste</a>

    {{ include('services/_delete_form.html.twig') }}
</div>
{% endblock %}
